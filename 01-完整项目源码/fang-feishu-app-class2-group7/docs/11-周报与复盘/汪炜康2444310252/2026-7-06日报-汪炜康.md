# 工作日报

**日期**：2026-07-06
**成员**：B
**角色**：前端开发（Web / PC）

---

## 一、今日完成

- **前端对接真后端**（`http://120.76.54.147`），按 P0–P4 五阶段分阶段交付，全部通过 `vue-tsc` + `vite build` 验证：

  | 阶段                 | 内容                                                                                                                                                                                                                                                                                                  |
  | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | **P0 地基**          | 重构请求层（统一响应包 `{code,message,data,traceId}` 解包 + `BizError`、401 自动登出、multipart 上传、Blob 下载）；统一 User 类型以 `/auth/me` 为准；auth store（`login` / `fetchCurrentUser` / `logout` / `isAdmin`）；路由守卫 + 侧边栏按角色渲染；移除废弃文件 `api/user.ts` 与 `utils/request.ts` |
  | **P1 通讯录 + 通知** | 部门树 + 成员卡片 + 详情抽屉 + 搜索；顶栏通知铃铛（未读角标 + 抽屉 + 轮询）                                                                                                                                                                                                                           |
  | **P2 CRUD 业务**     | 文档（列表 / 编辑 / 评论 / 版本）、云盘（上传带进度 / 下载 / 删除）、日历（范围查询 / 增改删）、审批（状态 Tab / 发起 / Admin 通过驳回）；工作台统计卡接真实数据                                                                                                                                      |
  | **P3 IM**            | 双栏会话/消息，发送 / 撤回 / 已读，5s 可见性感知轮询                                                                                                                                                                                                                                                  |
  | **P4 Admin 后台**    | 三 Tab：用户管理 / 角色管理 / 操作日志                                                                                                                                                                                                                                                                |

- 新增约 **20 个文件**（8 个 api 模块、8 个 types、3 个 store），重写 8 个业务页；**全程零新依赖**，沿用 Tailwind + CSS 变量体系。

- **真机联调**（服务器 `120.76.54.147`）：健康检查、`POST /api/v1/auth/login`、`GET /api/v1/auth/me`、`GET /api/v1/contacts`、`GET /api/v1/users` —— **全部通过**。验证后端 JSON 字段为 camelCase，与前端类型**逐字段吻合零偏差**；admin/user_a 角色差异生效（侧边栏、路由守卫、Admin 接口 403）。

- **UI 视觉升级**：
  - 登录 / 注册页改分屏式：密码切换、实时字段校验、记住我、密码强度条。
  - 工作台 / 设置页美化为"柔和卡片飞书风"：渐变欢迎横幅 + 时段问候、彩色渐变图标卡、hover 微交互、分段控件主题切换。
  - 全站留白收紧一档（页边距、卡片内距、栅格间隙），去掉大屏两侧大面积空白。

---

## 二、遇到问题

| #   | 问题                              | 当前处理                                                                         |
| --- | --------------------------------- | -------------------------------------------------------------------------------- |
| 1   | 真后端**无注册接口**              | 前端注册页 fallback 为友好错误提示，**待产品决策**：隐藏 / 改提示页 / 后端补接口 |
| 2   | 真后端**无 refresh-token 接口**   | 已删除 401 自动 refresh 逻辑；401 → 直接 `logout()` 跳登录页                     |
| 3   | IM 一期使用 5s 轮询，未接 SignalR | 可见性感知（隐藏页停轮询），演示场景可接受；正式上线需升级                       |
| 4   | 用户初次反馈"全站留白过多"        | 已统一收紧一档（`p-6 → p-4`、`gap-5 → gap-4`），待再次确认                       |

---

## 三、明日计划

1. 将"柔和卡片飞书风"视觉规范（渐变图标、彩色卡片、hover 微交互）铺到通讯录 / IM / 文档 / 云盘 / 日历 / 审批 / 管理后台，**统一全站观感**。
2. 与成员 C 协同：把 Web 端 axios 封装、auth store、权限判断抽成公共模块，便于 PC 端（Electron）与移动端（uni-app）复用。
3. 处理注册页最终形态（根据产品决策）。
4. 跟进联调中的所有反馈，按需修字段或交互。

---

## 四、需要配合

- **成员 A**
  - 确认注册功能最终形态（隐藏 / 提示页 / 后端补 `POST /api/v1/auth/register`）。
  - 若补注册接口，请提供请求 / 响应 DTO。

、

- **产品**
  - 注册功能决策（见上）。

---

**产出物清单**

- 新增文件：`src/types/{api,contact,notification,document,file,calendar,approval,im,admin}.ts`、`src/api/{contacts,notifications,documents,files,calendar,approvals,dashboard,im,admin}.ts`、`src/stores/{notifications,im}.ts`、`src/views/notifications/NotificationDrawer.vue`、`src/components/auth/AuthBrand.vue`
- 重写文件：`src/api/{index,auth}.ts`、`src/stores/auth.ts`、`src/router/index.ts`、`src/main.ts`、`src/components/layout/{MainLayout,Sidebar}.vue`、`src/views/{dashboard,settings,contacts,chat,document,drive,calendar,approval,admin}/*.vue`
- 配置改动：`vite.config.ts`（proxy 指向 `120.76.54.147`）、`.env`（`VITE_API_BASE_URL=/api/v1`）

**整体进度**：✅ 登录鉴权闭环已通 ｜ 🟡 业务页 UI 风格统一进行中 ｜ 🟢 全员阻塞点为零
