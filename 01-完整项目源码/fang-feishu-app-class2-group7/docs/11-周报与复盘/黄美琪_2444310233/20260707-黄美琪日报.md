# 日报 - 2026-07-07

**成员**：黄美琪（移动端开发 - uni-app）

## 一、今日完成

### 通讯录页面重写
- [x] 重构通讯录页面，替换原有简陋的 `showModal` 名片弹窗为自定义遮罩弹出卡片
- [x] 自定义名片卡片：渐变头像区 + 部门/职位/邮箱/手机 各占一行
- [x] 部门列表改为横向滚动 Chip 药丸片样式，选中态蓝色渐变高亮
- [x] 成员列表每行显示头像 + 姓名 + 部门标签 + 职位标签
- [x] 空状态美化（图标 + 提示文字）

### 中英文映射
- [x] 部门名称映射：Technology→技术部、Product→产品部、Operations→运营部 等
- [x] 职位映射：Project Lead→项目主管、Frontend Developer→前端开发、DevOps Engineer→运维工程师 等
- [x] 公司名映射：FangFeishu Demo Company→仿飞书 Demo 公司

### 部门树处理
- [x] 后端 API 返回的部门树含根节点 `FangFeishu Demo Company`，通过 `tree.length === 1 && tree[0].children?.length` 跳过根节点，直接取子部门展示

### 登录页完善
- [x] 记住账号密码功能（JSON 存储 username + password）
- [x] 密码眼睛切换 👁/🙈
- [x] 登录失败只清空密码、保留账号、显示错误提示
- [x] 登录成功跳转首页

### 首页功能
- [x] 通知铃铛 + 未读角标
- [x] 功能菜单网格（通讯录、消息、文档、云盘、日历、审批、管理后台）
- [x] 用户信息卡片 + 退出登录确认弹窗

### 请求封装优化
- [x] 登录页 401 不跳转登录页（避免死循环）

### 后端 API 联调验证
- [x] `POST /auth/login` — admin/123456 验证通过，返回 token + 用户信息
- [x] `GET /departments/tree` — 返回公司根节点 + 3个子部门
- [x] `GET /contacts` — 返回成员列表，含 departmentName、position 等字段
- [x] 确认后端接口已更新可用

## 二、遇到问题

1. **HBuilderX 编译器不兼容** — HBuilderX 1.3.3-2026041017 内置 uni-app 编译器与最新 alpha 包不兼容，无法通过 HBuilderX GUI 编译
2. **API 返回英文部门名** — 后端部门数据为英文，需要前端做中英文映射
3. **公司根节点显示问题** — `FangFeishu Demo Company` 在部门列表中作为可选部门出现，点击后无成员（成员在子部门）
4. **tabBar 字体大小** — 微信小程序原生 tabBar 不支持 `fontSize` 属性，无法直接调大字体

## 三、解决情况

1. 使用 CLI 编译（`npm run dev:mp-weixin` / `npm run dev:h5`）替代 HBuilderX GUI 编译
2. 前端实现 `toChineseName()` / `toChinesePosition()` 映射函数，覆盖所有已知部门和职位
3. 修改 `loadDepartments` 逻辑：检测根节点并跳过，直接展示子部门
4. 告知用户 tabBar 字体问题需使用自定义 tabBar 组件才能解决

## 四、明日计划

- [ ] 按前端草图完善包继续补齐其他页面（IM、文档、云盘、日历、审批、通知中心）
- [ ] 跟进后端接口变更，确保移动端各页面数据正常
- [ ] 编译小程序版本进行真机预览测试
