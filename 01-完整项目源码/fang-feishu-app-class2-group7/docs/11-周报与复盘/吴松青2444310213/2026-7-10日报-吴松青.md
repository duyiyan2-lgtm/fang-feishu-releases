# 日报 - 2026-07-10

**成员**：wsq（全栈开发）

## 今日完成
- [√] 配合前端完成通知模块联调，对齐 `GET /api/v1/notifications/unread` 聚合接口，新增按类型聚合的 `unread_count` 返回字段
- [√] 协助前端调试日历相关接口，修复 `POST /api/v1/calendar/events` 创建事件时 `start_time` 与 `end_time` 时区不一致问题
- [√] 配合前端打通审批流程联调，确认 `POST /api/v1/approvals`、`GET /api/v1/approvals/list`、`POST /api/v1/approvals/{id}/approve` 三个接口的入参与返回结构
- [√] 帮助前端同学处理审批回调与状态机不一致问题，补充审批当前节点和历史节点的返回字段
- [√] 跟进 IM 重连去重问题，补充 `message_id` 幂等字段并在 `WebSocket` 入口加入 `ack` 回执，确认前端重连后不再出现消息重复
- [√] 整理前后端联调通过的接口清单，更新接口文档错误码说明，补充每个接口的请求与返回示例

## 今日计划
- [ ] 整理本周联调过程中的接口变更问题，补充一份接口差异说明给前端同学
- [ ] 检查前后端联调覆盖度，补齐尚未走通的主流程接口回归用例

## 阻塞与求助
- 阻塞点：审批模块后端的 `approve` 和 `reject` 动作目前返回值缺少操作人 `avatar`，前端展示需要补充
- 需要支持：日历模块前端希望后端返回按周聚合的事件统计，后端当前按列表返回，需要确认是否新增聚合接口

## 明日计划
- [ ] 补齐审批 `approve`、`reject` 返回字段，补齐日历周视图聚合接口
- [ ] 继续配合前端做最后一轮联调回归，确保通知、日历、审批、IM 主流程完整可用
- [ ] 整理本周前端联调过程的问题与结论，准备提交一周工作留痕
