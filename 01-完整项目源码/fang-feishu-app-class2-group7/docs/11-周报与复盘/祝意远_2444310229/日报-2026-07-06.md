

日期：2026年7月6日  
角色：组长 / 后端负责人

## 一、今日完成

1. 完成后端项目运行与接口测试，确认后端服务、Swagger、数据库连接正常。
2. 测试了登录鉴权、当前用户、通讯录、IM 消息、文档、云盘、审批、日历、通知、操作日志等核心接口。
3. 模拟网站端、PC 客户端、手机端、微信小程序四端账号进行数据互通测试，验证四端可共用同一套后端 API 和数据库。
4. 修复测试中发现的文档更新接口和审批通过接口异常问题。
5. 整理完成《后端接口说明-四端联调版》，用于前端、后端、测试成员联调。
6. 明确文件模块采用本地 `storage` 存储，暂不启用 MinIO，保证 2核2G 服务器演示稳定。
7. 梳理阿里云服务器部署方案，明确由成员 E 负责 PostgreSQL、后端 API、Nginx 部署。
8. 明确各成员后续协作方式：B/C 负责前端和 uni-app，D 配合后端业务接口，E 负责部署，F 负责测试、文档和验收材料。

## 二、遇到问题

1. Swagger 测试时部分参数需要 GUID，普通字符串会导致参数校验失败。
2. 文档更新和审批通过接口测试时出现后端异常。
3. 服务器配置只有 2核2G，需要控制部署组件，避免资源占用过高。
4. Git 提交时部分文件路径和实际目录不一致，导致 `git add` 找不到文件。

## 三、解决情况

1. 已明确 GUID 参数使用方式，并写入接口说明文档。
2. 已修复文档更新和审批通过接口问题，并重新测试通过。
3. 已决定部署时不启用 MinIO，文件使用本地 `storage` 存储。
4. 已确认项目方案、后端核心项目和接口说明文档的实际存放位置。

## 四、明日计划

1. 将接口说明文档发给成员 B、C、D、F，开始前后端联调。
2. 配合成员 E 完成阿里云服务器部署。
3. 协助成员 B、C 配置前端请求 Base URL、Token 鉴权和接口调用。
4. 配合成员 D 继续完善接口参数校验、错误提示和业务细节。
5. 配合成员 F 编写测试用例、Bug 清单和验收材料。
6. 检查部署后的 Swagger 地址和核心接口是否可被组员访问。

## 五、需要配合

1. 成员 B/C：根据接口说明文档完成前端页面和四端接口调用。
2. 成员 D：协助处理后端业务接口问题。
3. 成员 E：负责阿里云服务器部署，并提供 Swagger 地址和 API Base URL。
4. 成员 F：根据接口说明整理测试用例、测试截图和验收文档。

