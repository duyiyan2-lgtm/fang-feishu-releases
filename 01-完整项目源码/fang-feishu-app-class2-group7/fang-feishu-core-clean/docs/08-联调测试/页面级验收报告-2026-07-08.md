# 小程序端页面级验收报告-2026-07-08

## 一、验收基本信息

| 项目 | 内容 |
| --- | --- |
| 验收日期 | 2026-07-08 |
| 验收阶段 | 小程序端前后端页面级验收 |
| 后端地址 | http://120.76.54.147 |
| API 基础地址 | http://120.76.54.147/api/v1 |
| 前置条件 | 登录、token、基础接口、五个核心业务模块接口均已联调通过 |
| 验收范围 | 小程序端 |

## 二、接口联调汇总

本轮小程序端页面验收前，已完成 18 个核心接口联调：

| 模块 | 接口数量 | 结果 |
| --- | --- | --- |
| IM | 3 | 通过 |
| 文档 | 5 | 通过 |
| 云盘 | 3 | 通过 |
| 日程 | 4 | 通过 |
| 审批 | 4 | 通过 |

结论：

```text
18 个核心业务接口全部通过，Status 为 200/201，返回 code=0。
```

## 三、小程序端页面级验收结果

### 1. IM 模块

| 检查项 | 结果 |
| --- | --- |
| 会话列表展示 | 通过，`onShow` 刷新，显示 `lastMessage.content` 和 `isRecalled` |
| 消息发送即时显示 | 通过，乐观更新后使用 `Object.assign` 替换真实 ID |
| 撤回显示 | 通过，可显示“你撤回了一条消息”或“对方撤回了一条消息” |
| SignalR 实时通信 | 通过，登录后执行 `signalR.connect()`，聊天页监听 `ReceiveMessage` |
| 时间格式 | 通过，今天显示 `HH:MM`，非今天显示 `MM/DD HH:MM` |

结论：

```text
IM 页面验收通过。
```

### 2. 文档模块

| 检查项 | 结果 |
| --- | --- |
| 文档列表展示 | 通过，`onShow` 刷新，按 `ownerId` 用户隔离 |
| 新建文档 | 通过，`navigateTo` 到 `id=0`，保存后调用 `POST /documents` |
| 编辑保存 | 通过，调用 `PUT /documents/{id}`，并刷新版本列表 |
| 评论发表 | 通过，调用 `POST /documents/{id}/comments`，并刷新评论列表 |
| 格式工具栏 | 通过，支持 B/I/U/H/列表/引用/代码 |

结论：

```text
文档页面验收通过。
```

### 3. 云盘模块

| 检查项 | 结果 |
| --- | --- |
| 文件列表展示 | 通过，`onShow` 刷新，按 `uploaderId` 用户隔离 |
| 文件上传 | 通过，进度条显示，调用 `POST /files/upload` 后刷新列表 |
| 文件下载 | 通过，携带 `Authorization` header，使用 `uni.saveFile` |
| 文件删除 | 通过，二次确认后调用 `DELETE /files/{id}` 并刷新列表 |
| 下载 URL | 风险提示：当前使用绝对 URL `http://120.76.54.147/...`，H5 下可能跨域，小程序端无影响 |

结论：

```text
云盘页面验收通过；H5 下载跨域作为后续优化项。
```

### 4. 日程模块

| 检查项 | 结果 |
| --- | --- |
| 日程列表展示 | 通过，支持周导航和日期芯片，按 `userId` 用户隔离 |
| 新增日程 | 通过，弹窗提交后调用 `POST /calendar/events` 并刷新 |
| 编辑日程 | 通过，回填数据后调用 `PUT /calendar/events/{id}` 并刷新 |
| 删除日程 | 通过，二次确认后调用 `DELETE /calendar/events/{id}` 并刷新 |

结论：

```text
日程页面验收通过。
```

### 5. 审批模块

| 检查项 | 结果 |
| --- | --- |
| 我的申请 / 审批管理 | 通过，`onShow` 刷新，按 `applicantId` 隔离 |
| 提交请假 | 通过，弹窗提交后调用 `POST /approvals` 并刷新 |
| 通过审批 | 通过，详情抽屉中调用 `PATCH /approvals/{id}/approve` 并刷新 |
| 驳回审批 | 通过，详情抽屉中调用 `PATCH /approvals/{id}/reject` 并刷新 |

结论：

```text
审批页面验收通过。
```

### 6. 通知中心

| 检查项 | 结果 |
| --- | --- |
| 通知列表 | 通过，`onShow` 刷新，后端按 token 用户隔离 |
| 类型筛选 | 通过，支持全部、聊天、审批、文档、系统筛选 |
| 单条标已读 | 通过，调用 `PATCH /notifications/{id}/read` |
| 全部已读 | 通过，调用 `PATCH /notifications/read-all` |
| 跳转 | 通过，聊天跳 IM，审批跳审批，文档跳文档 |

结论：

```text
通知中心页面验收通过。
```

### 7. 管理后台

| 子页面 | 结果 |
| --- | --- |
| 用户管理 | 通过，支持搜索、新增、编辑、启用禁用、分页 |
| 部门管理 | 通过，支持部门树、新增子部门、编辑、删除 |
| 角色管理 | 通过，支持列表、新增，字段使用 PascalCase |
| 操作日志 | 通过，支持按操作人、模块、日期多条件筛选和分页 |

结论：

```text
管理后台页面验收通过。
```

## 四、遗留风险

| 编号 | 风险 | 影响 | 建议 |
| --- | --- | --- | --- |
| RISK-001 | 云盘下载使用绝对 URL `http://120.76.54.147/...` | H5 下可能出现跨域问题，小程序端当前无影响 | 后续如需 H5 演示，建议统一走后端下载接口或配置跨域策略 |

## 五、总体验收结论

本次验收结论仅针对小程序端：

```text
18 个核心接口全部通过，小程序端 7 个页面模块验收通过。
```

小程序端已通过页面模块：

| 序号 | 页面模块 | 结果 |
| --- | --- | --- |
| 1 | IM | 通过 |
| 2 | 文档 | 通过 |
| 3 | 云盘 | 通过 |
| 4 | 日程 | 通过 |
| 5 | 审批 | 通过 |
| 6 | 通知中心 | 通过 |
| 7 | 管理后台 | 通过 |

当前阶段状态：

```text
前后端业务接口联调已阶段通过，小程序端页面级验收已通过。其他端（如 H5/PC Web）仍需按对应端单独验收。
```
