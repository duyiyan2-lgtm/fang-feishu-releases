# 前后端联调记录-2026-07-07

## 一、联调基本信息

| 项目 | 内容 |
| --- | --- |
| 联调日期 | 2026-07-07 |
| 后端服务器 | http://120.76.54.147 |
| API 基础地址 | http://120.76.54.147/api/v1 |
| Swagger 地址 | http://120.76.54.147/swagger |
| SignalR 地址 | http://120.76.54.147/hubs/im |
| 测试账号 | admin / 123456，user_a / 123456，user_b / 123456 |
| 当前阶段 | 前后端第一轮联调 |

## 二、后端部署验证

| 序号 | 验证项 | 验证结果 | 说明 |
| --- | --- | --- | --- |
| 1 | Docker 容器状态 | 通过 | `api`、`postgres`、`minio` 均为 `Up`，PostgreSQL 为 `healthy` |
| 2 | 本机健康检查 | 通过 | `curl http://127.0.0.1:5080/health` 返回 `Healthy` |
| 3 | 公网健康检查 | 通过 | 浏览器访问 `http://120.76.54.147/health` 返回 `Healthy` |
| 4 | Swagger 页面 | 通过 | `http://120.76.54.147/swagger` 可正常打开并显示接口列表 |
| 5 | Nginx 配置检查 | 通过 | `nginx -t` 显示配置语法正确 |
| 6 | 登录接口服务器测试 | 通过 | `POST /api/v1/auth/login` 可返回 `token` |

## 三、前端配置确认

| 序号 | 检查项 | 当前结果 | 说明 |
| --- | --- | --- | --- |
| 1 | 前端 API 地址 | 通过 | 已配置为 `http://120.76.54.147/api/v1` |
| 2 | Mock 开关 | 通过 | `USE_MOCK = false`，已关闭 Mock，开始请求真实后端 |
| 3 | 登录接口返回 | 通过 | Network 中 `login` 返回 `code: 0`、`message: success` |
| 4 | Token 保存 | 通过 | 登录成功后 `data.token` 已存入本地缓存 |
| 5 | 后续请求鉴权头 | 通过 | 请求已统一自动携带 `Authorization: Bearer token`，`GET /contacts?page=1&pageSize=20` 已截图验证 |
| 6 | 请求超时时间 | 通过 | 前端请求超时时间已调整为 15000ms |
| 7 | 小程序开发工具域名校验 | 通过 | 本地设置中已勾选“不校验合法域名、web-view 业务域名、TLS 版本以及 HTTPS 证书” |
| 8 | 热重载 WebSocket | 通过 | `ws://192.168.3.2:8090/mp-weixin_rtblq4` 为 uni-app 开发热重载连接，状态 `101 Switching Protocols`，不属于后端业务接口 |

## 四、模块联调记录

| 序号 | 模块 | 前端页面 | 后端接口 | 测试账号 | 负责人 | 测试结果 | 问题描述 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 登录 | 登录页 | `POST /auth/login` | admin | 前端 | 通过 | 已关闭 Mock，baseURL 指向服务器，接口返回 `code=0`，token 已存入本地缓存 | 已完成 |
| 2 | 当前用户 | 首页 / 个人信息 | `GET /auth/me` | admin | 前端 | 通过 | 组员反馈接口返回 `200 / code=0`，请求已自动携带 `Authorization: Bearer token` | 已完成 |
| 3 | 未读通知数 | 首页 / 通知入口 | `GET /notifications/unread-count` | admin | 前端 | 通过 | 组员反馈接口返回 `200 / code=0`，请求已自动携带 `Authorization: Bearer token` | 已完成 |
| 4 | 通讯录 | 通讯录页面 | `GET /contacts?page=1&pageSize=20` | admin / user_a | 前端 | 通过 | Network 显示状态码 `200 OK`，请求头已带 `Authorization: Bearer token`，Response 返回 5 条用户数据：admin、user_a、user_b、user_c、user_d | 已完成 |
| 5 | 部门树 | 通讯录 / 组织架构 | `GET /departments/tree` | admin / user_a | 前端 | 通过 | 组员反馈接口返回 `200 / code=0`，可作为通讯录组织结构基础数据 | 已完成 |
| 6 | 用户管理 | 管理后台用户页 | `GET /users` | admin | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 7 | 角色管理 | 管理后台角色页 | `GET /roles` | admin | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 8 | IM 会话 | 聊天列表 | `GET /im/conversations` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 9 | IM 消息 | 聊天窗口 | `GET /im/conversations/{id}/messages` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 10 | 发送消息 | 聊天窗口 | `POST /im/messages` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 11 | 文档列表 | 文档页面 | `GET /documents` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 12 | 新建文档 | 文档编辑页 | `POST /documents` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 13 | 文件列表 | 云盘页面 | `GET /files` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 14 | 文件上传 | 云盘页面 | `POST /files/upload` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 15 | 日程列表 | 日历页面 | `GET /calendar/events` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 16 | 新建日程 | 日历页面 | `POST /calendar/events` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 17 | 审批列表 | 审批页面 | `GET /approvals` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 18 | 提交审批 | 审批页面 | `POST /approvals` | user_a | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 19 | 审批处理 | 管理员审批页 | `PATCH /approvals/{id}/approve` / `PATCH /approvals/{id}/reject` | admin | 前端 | 待测 | 暂未开始页面联调 | 待联调 |
| 20 | 操作日志 | 管理后台日志页 | `GET /operation-logs` | admin | 前端 | 待测 | 暂未开始页面联调 | 待联调 |

## 五、问题记录

| 编号 | 发现时间 | 模块 | 问题描述 | 初步判断 | 责任人 | 处理建议 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | 2026-07-07 | 前端访问 | 前端视频中出现 `HTTP ERROR 502` | 当时可能访问了错误路径或代理路径，后端 `/health` 和 `/auth/login` 实测正常 | 前端 | 确认访问地址必须使用 `http://120.76.54.147/api/v1` 作为接口基础地址 | 已定位 |
| BUG-002 | 2026-07-07 | 通讯录 | 通讯录页面控制台出现 `Error: timeout` | 现已确认 `GET /contacts?page=1&pageSize=20` 返回 `200 OK`，已携带 `Authorization: Bearer token`，Response 返回 5 条用户数据 | 前端 | 若页面仍不显示，再排查前端字段映射；接口侧已通过 | 已解决 |
| BUG-003 | 2026-07-07 | 小程序网络环境 | 微信开发者工具控制台再次出现 `Error: timeout`，同时曾提示未校验合法域名、TLS 版本和 HTTPS 证书 | 已定位 timeout 来源为 uni-app / 微信开发者工具内部热重载 WebSocket：`ws://192.168.3.2:8090/mp-weixin_rtblq4`，状态 `101 Switching Protocols`，不属于后端业务接口 | 前端 | 业务接口全部正常返回时可忽略该开发工具内部提示；若正式真机或发布，仍需配置 HTTPS 域名 | 已定位，不影响业务 |
| INFO-001 | 2026-07-07 | 业务接口汇总 | 组员反馈基础业务接口均已正常返回 | `POST /auth/login`、`GET /auth/me`、`GET /contacts`、`GET /departments/tree`、`GET /notifications/unread-count` 均为 `200 / code=0` | 前端 / 后端 | 基础登录、鉴权、通讯录、部门树、通知未读数链路可标记通过，继续推进 IM、文档、云盘、日程和审批 | 已通过 |
| WARN-001 | 2026-07-07 | 小程序布局 | 控制台提示 `<scroll-view>` 需要设置 `enable-flex` | uni-app 布局相关警告，不影响接口联调主流程 | 前端 | 后续在对应 `scroll-view` 上补 `enable-flex="true"` | 待优化 |
| WARN-002 | 2026-07-07 | 小程序合法域名 | 微信开发者工具提示未校验合法域名、web-view 业务域名、TLS 版本和 HTTPS 证书 | 当前使用 `http://120.76.54.147` 适合开发阶段，正式真机和发布需要 HTTPS 域名；开发者工具本地校验已关闭 | 前端 / 运维 | 开发阶段已完成设置；正式演示前准备域名和 HTTPS 证书 | 已处理，正式环境待优化 |

## 六、下一步联调要求

前端下一步需要优先补充以下验证截图：

| 序号 | 验证内容 | 要求 |
| --- | --- | --- |
| 1 | Token 保存 | 已确认登录成功后 `data.token` 已保存到本地缓存 |
| 2 | 当前用户接口 | 已由组员反馈 `GET /auth/me` 返回 `200 / code=0` |
| 3 | 请求头 | 已确认后续接口统一携带 `Authorization: Bearer token` |
| 4 | 通讯录接口 | 已确认 URL、Status、Headers、Response，接口返回 5 条用户数据 |
| 5 | 超时问题 | 已定位为 uni-app / 微信开发者工具热重载 WebSocket，不属于业务接口；业务接口可继续联调 |
| 6 | 小程序域名校验 | 已确认开发者工具中关闭合法域名、TLS 和 HTTPS 证书校验 |
| 7 | 下一批业务模块 | 继续推进 IM、文档、云盘、日程、审批、操作日志 |

## 七、当前阶段结论

截至 2026-07-07，本轮联调结论如下：

- 后端服务器部署验证已通过。
- 前端已将 baseURL 指向真实后端，并关闭 Mock。
- 登录接口 `POST /auth/login` 已通过。
- Token 保存和后续请求鉴权头已验证，`GET /auth/me` 和 `GET /notifications/unread-count` 已由组员反馈返回 `200 / code=0`。
- 通讯录接口已通过，`GET /contacts?page=1&pageSize=20` 返回 `200 OK`，并返回 5 条用户数据。
- 部门树接口 `GET /departments/tree` 已由组员反馈返回 `200 / code=0`。
- Console 中的 timeout 已定位为 uni-app / 微信开发者工具内部热重载 WebSocket，不属于后端业务接口，不影响联调进度。
- 小程序开发工具本地合法域名、TLS 和 HTTPS 证书校验已关闭，开发阶段可继续使用 `http://120.76.54.147` 联调。

当前阶段状态：

```text
前后端联调已开始；登录、token 缓存、自动鉴权头、auth/me、通讯录、部门树、通知未读数均已通过；timeout 已定位为开发工具内部热重载连接；下一步推进 IM、文档、云盘、日程、审批和操作日志模块。
```
